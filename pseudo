header:
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">


footer:
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>



homepage:
Use list of popular tags to create clickable cards
    when one is clicked, query the database for routines with that tags                                 // INDEX ROUTINES
        index a list of those routines, each one should be clickable
            when clicked, show the following about the routine:                                         // SHOW ROUTINESINE
                creator
                workout and exercise data
                creator tips
                reviews/comments
                if logged in    
                    Able to write a comment.
                    if comment is filled,
                        enable Submit comment/reviews                                                   // adding a comment would be a .save?
Have a create routine button
    When clicked, show form to make a new Routine.                                                      // NEW ROUTINE
        user will add name of routine
        user will add how many days (X) in the routine before it repeats. (<7)
        (icebox/optional) user can enter tips for their workout routine (Every other day, last set AMRAP, this is for beginners/advanced etc.)
        then button will enable, taking user to create workout data.
            when clicked, show form to make a new workout with 'X' amount of workouts to fill out.
                user must fill in each box.
                When all filled in, enable create button
                    When clicked, show the corresponding workoutpage                                    // CREATE


on create for routine, redirect to workout/new 


icebox (appendChild to html if I want to )